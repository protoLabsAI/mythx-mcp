/**
 * Situations Module
 *
 * Shared resolution of session situations (generated content with
 * world-pack fallback).
 */

export { resolveRawSituations, type SituationSourceSession } from "./resolve-situations.js";
